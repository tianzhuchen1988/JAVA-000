spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/sharding_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT
    username: root
    password:

mybatis:
  type-aliases-package: com.example.demo.mapper
  mapperLocations: classpath:mapper/*Mapper.xml